import { SafeAreaView, StatusBar, Text } from 'react-native'
import styles from './styles'

const Swiper = () => (
  <SafeAreaView style={styles.container}>
    <StatusBar />
    <Text>Swiper is coming...</Text>
  </SafeAreaView>
)

export default Swiper
