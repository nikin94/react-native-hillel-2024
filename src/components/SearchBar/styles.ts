import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 10,
    width: '100%',
    marginBottom: 8,
    alignItems: 'center'
  },
  inputContainer: {
    flex: 1
  }
})

export default styles
