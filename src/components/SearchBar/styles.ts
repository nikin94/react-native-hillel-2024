import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    height: 40,
    gap: 10,
    width: '100%',
    marginBottom: 8
  },
  inputContainer: {
    flex: 1
  }
})

export default styles
